<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trainings</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            color: #333;
        }

        header {
            background-color: #007bff;
            color: white;
            padding: 15px;
            text-align: center;
        }

        nav a {
            color: white;
            text-decoration: none;
            font-weight: bold;
        }

        main {
            padding: 20px;
        }

        h2 {
            text-align: center;
            color: #333;
        }

        .training {
            background: white;
            margin: 20px auto;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 90%;
            min-height: 100x; /* גובה קבוע */
            display: flex;
            flex-direction: column;
            justify-content: center;
            text-align: center;
        }

        .training h3 {
            color: #007bff;
            margin: 10px 0;
        }

        .training p {
            margin: 8px 0;
        }

        .separator {
            border: none;
            height: 4px;
            background: linear-gradient(to right, #007bff, #f9f9f9);
            margin: 20px auto;
            width: 90%;
        }

        button {
            padding: 8px 12px;
            border: none;
            border-radius: 5px;
            color: white;
            cursor: pointer;
            font-weight: bold;
        }

        button.reply-btn { background-color: #007bff; }
        button.delete-btn { background-color: #dc3545; }
        button.save-btn { background-color: #28a745; }

        textarea {
            width: 90%;
            padding: 10px;
            margin-top: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
        }
    </style>
    
</head>
<body>
    <header>
        <h1>Welcome, <%= username %>!</h1>
        <nav>
            <a href="/index1.html">Back to Home</a>
        </nav>
    </header>
 
        <section>
            <h2>Trainings</h2>
            <% if (trainings.length > 0) { %>
                <% trainings.forEach(training => { %>
                    <div class="training">
                        <h3><%= training.username %></h3>
                        <p><strong>Time:</strong> <%= training.training_time %></p>
                        <p><strong>Workout Type:</strong> <%= training.workout_type %></p>
                        <div class="replies">
                            <p><strong>Replies:</strong></p>
                            <% if (training.replies && training.replies.length > 0) { %>
                                <ul>
                                    <% training.replies.forEach(reply => { %>
                                        <li>
                                            <span><%= reply.username %>:</span> <%= reply.reply_text %>
                                            <br><small><%= new Date(reply.created_at).toLocaleString() %></small>
                                            
                                        
                                        <form action="/delete-replyt" method="POST" style="display: inline;">
                                            <input type="hidden" name="replyId" value="<%= reply.id %>">
                                            <button 
                                                type="submit" 
                                                style="margin-top: 10px; background-color: red; color: white; border: none; padding: 10px; border-radius: 5px; cursor: pointer;">
                                                Delete Reply
                                            </button>
                                        </form>
                                        <button onclick="toggleEditReplyForm('<%= reply.id %>')" 
                                            style="background: blue; color: white; border: none; padding: 8px 15px; cursor: pointer; border-radius: 5px;">
                                            Edit
                                        </button>
                                        
                                        <!-- טופס לעריכת תגובה -->
                                        <form id="edit-reply-form-<%= reply.id %>" style="display: none; margin-top: 10px;" method="POST" action="/edit-replyt">
                                            <textarea 
                                                name="reply" 
                                                rows="3" 
                                                style="width: 100%; border-radius: 5px; padding: 10px; border: 1px solid #ccc;" 
                                                placeholder="Edit your reply here..."><%= reply.reply_text %></textarea>
                                            <input type="hidden" name="replyId" value="<%= reply.id %>">
                                            <button 
                                                type="submit" 
                                                style="margin-top: 10px; padding: 8px 15px; background: green; color: white; border: none; border-radius: 5px; cursor: pointer;">
                                                Save Changes
                                            </button>
                                        </form>
                                    </li>
                                    <% }); %>
                                </ul>
                            <% } else { %>
                                <p>No replies yet.</p>
                            <% } %>
                        </div>
                      
                        
                        <!-- טופס להוספת תגובה לאימון זה -->
                        <div class="reply-section">
                            <!-- כפתור להצגת טופס תגובה -->
                            <button onclick="toggleReplyForm('<%= training.id %>')" style="margin-top: 10px; padding: 10px; border-radius: 5px; background-color: #007bff; color: white; border: none; cursor: pointer;">
                                Reply
                            </button>
                
                            <!-- טופס תגובה -->
                            <form id="reply-form-<%= training.id %>" style="display: none; margin-top: 10px;" method="POST" action="/replyt">
                                <textarea 
                                    name="reply" 
                                    rows="3" 
                                    style="width: 100%; border-radius: 5px; padding: 10px; border: 1px solid #ccc;" 
                                    placeholder="Write your reply here..."></textarea>
                                <input type="hidden" name="trainingId" value="<%= training.id %>">
                                <button 
                                    type="submit" 
                                    style="margin-top: 10px; padding: 10px; border-radius: 5px; background-color: #28a745; color: white; border: none; cursor: pointer;">
                                    Submit Reply
                                </button>
                            </form>
                        </div>
                        <div class="delete-section" style="margin-top: 10px;">
                            <!-- טופס למחיקת משוב -->
                            <div class="delete-section" style="margin-top: 10px;">
                                <form method="POST" action="/delete-trainings" onsubmit="return confirm('Are you sure you want to delete this workout?');">
                                    <input type="hidden" name="trainingId" value="<%= training.id %>">
                                    <button 
                                        type="submit" 
                                        class="delete-button" 
                                        style="
                                            background-color: #dc3545; 
                                            color: white; 
                                            padding: 8px 15px; 
                                            border: none; 
                                            border-radius: 5px; 
                                            cursor: pointer;
                                            font-size: 14px;">
                                        Delete Workout
                                    </button>
                                </form>
                            </div>
                    

                        
                <% }); %>
            <% } else { %>
                <p>No trainings found.</p>
            <% } %>
        </section>
    
    <script>
        function toggleReplyForm(trainingId) {
            const form = document.getElementById(`reply-form-${trainingId}`);
            if (form.style.display === 'none' || form.style.display === '') {
                form.style.display = 'block'; // מציג את הטופס
            } else {
                form.style.display = 'none'; // מסתיר את הטופס
            }
        }
    </script>
    <script>
        // פונקציה להצגת/הסתרת טופס עריכה
        function toggleEditReplyForm(replyId) {
            const form = document.getElementById(`edit-reply-form-${replyId}`);
            if (form.style.display === 'none' || form.style.display === '') {
                form.style.display = 'block'; // מציג את הטופס
            } else {
                form.style.display = 'none'; // מסתיר את הטופס
            }
        }
    </script>
</body>
</html>
     
